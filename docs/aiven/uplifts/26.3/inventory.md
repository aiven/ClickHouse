# 26.3 uplift — patch inventory

Mechanical inventory produced by the T2 classifier subagent. One row per
commit in `v25.8.18.1-lts..origin/v25.8.18.1-lts-aiven` at classification
time. No judgment of testability, complexity, or porting priority — those
decisions are made fresh per dispatch in T3+.

> **Source-branch re-check (2026-06-12).** The Aiven source tag advanced
> `v25.8.18.1-lts-aiven` → `v25.8.24.21-lts-aiven`. A read-only comparison of the
> two branches' commit lists (GitHub, no fetch/push) shows an **identical Aiven
> patch stack** — same tip (`Enforce protected-user checks before ON CLUSTER
> dispatch`), same ordering down to `045` — re-dated to a single rebase day. That
> is the stack rebased onto a newer **upstream point-release base**, with **no new
> Aiven patches added on top**. This inventory (built from `v25.8.18.1-lts-aiven`)
> therefore stays complete; no new rows. The upstream `25.8.19`→`25.8.24` fixes are
> already contained in our `v26.3.10.62-lts` base, so the bump is inert for this
> uplift. (Aside: the branch tip `Enforce protected-user checks before ON CLUSTER
> dispatch`, `f4552b14c77`, has no dedicated row — it is folded into row `022`; its
> content is confirmed present in our branch via `patch-port(022)`.)

## How to read this inventory

- **`NNN`** is a stable 1-based index, sorted by committer-date ascending
  (oldest commit on the branch = `001`). For most patches this matches the
  `date` column closely; for cherry-picks from upstream (e.g. row 074), the
  `date` (= author date) can be much older than the position implies.
- **`date`** is the author date (`git log --format=%ad --date=short`),
  NOT the committer date. For native Aiven patches the two are close; for
  upstream-authored cherry-picks they may diverge by months.
- **`author`** is the **author** email (`git log --format=%ae`) — i.e., the
  person who originally wrote the patch. This is the right column to consult
  when asking "who designed this?". Author attribution is preserved across
  the 25.3 → 25.8 → 26.3 porting chain; if it shows tilman, tilman wrote it.
  T2.2 (post-rebase) data shows: 63 tilman, 10 khatskevich, 4 joelynch,
  1 vitlibar (the vitlibar row is upstream-authored — see note on patch 074
  in the retrospective).
- **`committer`** is the **committer** email (`git log --format=%ce`) — i.e.,
  the person who landed this SHA on the `v25.8.18.1-lts-aiven` branch. Useful
  for "who to ask about the integration / merge" as opposed to "who designed
  it". After the May 21 force-push of `v25.8.18.1-lts-aiven`, the committer
  is almost entirely `joelynch112@gmail.com` (57) or
  `alex.khatskevich@aiven.io` (21). Pre-rebuild SHAs had committer mostly
  `tilman.moeller@aiven.io` (the 25.3 → 25.8 porting committer); the rebuild
  re-attributed committer to whoever landed each patch on the release branch.
- **`cherry_pick_clean`** is `yes` if `git format-patch -1 <sha> --stdout |
  git apply --check` exits 0 against the current HEAD on
  `v26.3.10.62-lts-aiven-dev` (the readonly equivalent of a cherry-pick
  dry-run). `no` means the patch would conflict at apply time. `error`
  means metadata extraction failed for that commit (none on this run).
- **`files`** and **`loc`** are from `git diff-tree --numstat`. `loc` is
  added + removed lines (i.e., total line churn, not net delta).
- **Linked `NNN`** — when an `NNN` cell is a markdown link (e.g.
  `[007](../patches/007-...)`), the linked file is the patch's dossier and
  is the source of truth for the per-uplift port outcome. The dossier's §6
  records whether the patch was shipped byte-equivalent, conflict-resolved,
  drift-superseded, or dropped — and why. The inventory itself stays
  mechanical (it does not duplicate outcomes); follow the link.

## Range justification

Source range: `v25.8.18.1-lts..origin/v25.8.18.1-lts-aiven`. This is the
"patches Aiven added to the prior LTS" range (78 commits), NOT
`v26.3.10.62-lts..origin/v25.8.18.1-lts-aiven` (which would be 944 commits
because it sweeps in ~866 upstream stable-branch backports that are not
Aiven patches). Per release-management decision 2026-05-22:
`v25.8.18.1-lts-aiven` is the current production aiven LTS line.

## Classification history

- **T2.1 (2026-05-22T12:24:05Z)** — first classifier dispatch against
  `v25.8.18.1-lts-aiven` at tip `3f401037cfb` ("Hide secrets in
  system.mutations.command column"). Count: 77 patches. Captured 8 columns
  (NNN, sha, date, author, files, loc, subject, cherry_pick_clean).
- **2026-05-21 (after T2.1 data collection but before commit)** —
  `v25.8.18.1-lts-aiven` was **force-pushed** by release management, rebuilt
  with proper `Author / Committer` distinction (committer is now the person
  who landed the patch on the release line, not the porter). One additional
  patch landed ("Fix MV refresh task race condition" = the previously
  pending `khatskevich/mv_race_258` work). New tip: `ea9c5d6420f`. All 77
  prior SHAs changed.
- **T2.2 (2026-05-22, later)** — re-classifier dispatch against the new tip.
  Count: 78. Same 25 `cherry_pick_clean=yes` / 53 `no` shape. Added a
  `committer` column (9 columns total) to surface the
  author-vs-committer distinction that the rebuild made meaningful.

The table below reflects T2.2 (current state). All SHAs are stable against
the current `origin/v25.8.18.1-lts-aiven` tip until the next force-push.

## Pending merges (out of scope for this inventory)

After the May 21 rebuild:

- `khatskevich/mv_race_258` — **merged** into `v25.8.18.1-lts-aiven` as row
  078 ("Fix MV refresh task race condition", `ea9c5d6420`). No longer pending.
- `khatskevich/peerdb_258` — still unmerged. After the force-push of
  `v25.8.18.1-lts-aiven`, this branch's tip is no longer a descendant of
  the production line, so `git rev-list --count
  origin/v25.8.18.1-lts-aiven..origin/khatskevich/peerdb_258` reports 58
  commits unique to it — but those are mostly the *old* (pre-rebuild) line
  re-surfacing; only the genuinely-new patches will need to be re-classified
  once `peerdb_258` is rebased onto the new tip.

When `peerdb_258` lands, re-dispatch the classifier; the inventory
regenerates idempotently and the only diff will be the new row(s).

## Classification metadata

- T2.1 classifier dispatch: 2026-05-22T12:24:05Z (subagent id `toolu_019bjhJRVovPjShrqdjWCitE`)
- T2.2 classifier dispatch: 2026-05-22 (re-run after force-push)
- Source range count: 78 (`git rev-list --count v25.8.18.1-lts..origin/v25.8.18.1-lts-aiven`)
- Classifier exit-toolchain: `git format-patch -1 <sha> --stdout | git apply --check`
- See also: T2 retrospective at `docs/aiven/uplifts/26.3/01-t2-classifier-retrospective.md`

## Status annotations (per-uplift overlay)

The inventory's columns are immutable T2-classifier output. Per-uplift outcomes (ported, dropped) are NOT a separate column — adding one would require migrating all 77 rows on every uplift. Instead, per-uplift status is recorded **inline in the `subject` column** using markdown conventions:

- **Ported (default):** subject text unchanged. The patch's dossier (`docs/aiven/patches/<NNN>-<slug>.md`) is the source of truth for the porting outcome.
- **Dropped (`obsoleted-by-upstream` or `irrelevant-by-removal`):** subject text is wrapped in `~~strikethrough~~`, followed by **bold annotation** stating the reason, the upstream SHA (for `obsoleted-by-upstream`) or the removed feature (for `irrelevant-by-removal`), and a relative-path link to the dossier.

Pattern (used for row 001 in this uplift):

> ~~Advertise host from config for replicated databases~~ **DROPPED — `obsoleted-by-upstream`. Upstream `9dd658aea06` landed equivalent fix. See dossier [`001-advertise-host-from-config.md`](../../patches/001-advertise-host-from-config.md).**

Rationale: dropped rows are the load-bearing case (the patch will never carry through any future uplift); ported rows are routine progress. Annotating only the load-bearing case keeps the inventory readable. The dossier carries the durable record either way.

## Inventory

| NNN | sha | date | author | committer | files | loc | subject | cherry_pick_clean |
|-----|-----|------|--------|-----------|-------|-----|---------|-------------------|
| 001 | ac84fa6f7c | 2025-12-02 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 1 | 3 | ~~Advertise host from config for replicated databases~~ **DROPPED — `obsoleted-by-upstream`. Upstream `9dd658aea06` landed equivalent fix. See dossier [`001-advertise-host-from-config.md`](../../patches/001-advertise-host-from-config.md).** | no |
| 002 | fdc262dc9d | 2025-12-04 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 3 | 27 | ~~Enable internal replication for DatabaseReplicated clusters~~ **DROPPED — `obsoleted-by-upstream`.** Upstream 26.3 ships the whole mechanism via a per-DB setting `DatabaseReplicatedSettings::internal_replication` (default `false`, present in base `v26.3.10.62-lts`, absent on 25.8): the value is threaded as an explicit `Cluster` ctor arg (`DatabaseReplicated.cpp:512`) into the same `addShard` insert-path logic the patch added (`Cluster.cpp:645,671-676`, byte-identical). The 25.8 patch hard-coded `internal_replication=true` on `ClusterConnectionParameters` only because the base had no such setting. Replacement is **config, not code**: set `<database_replicated><internal_replication>true</internal_replication></database_replicated>` in Aiven's managed config — it overrides the `DECLARE` default for existing+new Replicated DBs across all creation paths (precedence `DatabaseReplicated.cpp:2670-2673`). **Safe only with patch 004** (all-`ReplicatedMergeTree`); §5 handover. See dossier [`002-enable-internal-replication-for-databasereplicated-clusters.md`](../../patches/002-enable-internal-replication-for-databasereplicated-clusters.md) and ledger entry REPL-1 in [`major-upstream-changes.md`](major-upstream-changes.md). | no |
| 003 | 1055b0defe | 2025-12-04 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 4 | 29 | Enable ALTER DATABASE MODIFY SETTING for Replicated databases | no |
| 004 | 226ed6cc31 | 2025-12-05 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 2 | 30 | Replace MergeTree with ReplicatedMergeTree in Replicated databases | yes |
| 005 | 6a37150173 | 2025-12-06 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 1 | 10 | Tolerate ZooKeeper restart with increased retries and exponential backoff | no |
| 006 | 22e03c9d9d | 2025-12-07 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 1 | 2 | Fix ClickHouse restart with replicated tables containing {shard} macro | yes |
| 007 | 5228bf2cd4 | 2025-12-07 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 1 | 1 | ~~Add missing settings to recoverLostReplica~~ **DROPPED — `irrelevant-by-removal`. Upstream removed the `DEFLATE_QPL` setting this patch re-added in `recoverLostReplica`. See dossier [`007-recover-lost-replica-deflate-qpl-setting.md`](../../patches/007-recover-lost-replica-deflate-qpl-setting.md).** | yes |
| 008 | d6e78ab993 | 2025-12-07 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 13 | 287 | Fix unbounded replication queue growth — **PORTED `patch-port(008)`, `still-needed-but-rewrite` + AUGMENT.** Carried verbatim plus a new default-off server master guard `aiven_enable_replication_queue_size_limit` (clause-(v) blast-radius resolution; `queue_size_monitor` + the four queue thresholds carried untouched). Drift fixes: `createTask` 3-arg (StorageID), `LoggerPtr` via `getLogger`, and a 26.3 `Coordination::setCurrentComponent` guard in the monitor thread (else `LOGICAL_ERROR` "Current component is empty"). Ships integration test `test_aiven_replication_queue_size_limit` (2-node guard-on/guard-off differential: guard-ON INSERT rejected `LIMIT_EXCEEDED` "Too large replication queue", guard-OFF INSERT succeeds). See dossier [`008-replication-queue-size-limit.md`](../../patches/008-replication-queue-size-limit.md). | no |
| 009 | 110900c986 | 2025-12-07 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 1 | 14 | ~~Replicate ALTER TABLE MOVE PARTITION queries through DatabaseReplicated~~ **DROPPED — `not-justified` (net-negative on single-shard `ReplicatedMergeTree`, Aiven's only affected topology).** `MOVE … TO TABLE` is a **leader-only** DDL task (`taskShouldBeExecutedOnLeader`, `DDLWorker.cpp:807-818`), so one replica executes and peers converge via table replication **with or without** the patch — no data benefit (the data-loss-race justification was RETRACTED; it falsely assumed every-replica re-execution). The patch's only residual effect is DB-DDL-log serialization, but routing through the DDL log **amplifies a benign `MOVE` failure into a DB-DDL-queue head-of-line stall + forced digest-reset recovery** (`DatabaseReplicatedWorker.cpp:100-117`) — a new regression absent on the local-execution path. Residual risk: original incident not located (patch ships in the 25.8 stack); de-risk handover in dossier §6. See dossier [`009-replicate-move-partition-through-database-replicated.md`](../../patches/009-replicate-move-partition-through-database-replicated.md). | yes |
| 010 | 199db08799 | 2025-12-08 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 1 | 2 | Change default logs_to_keep from 1000 to 300 for DatabaseReplicated | no |
| 011 | 654f61e864 | 2025-12-09 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 2 | 14 | Restrict SHOW CREATE DATABASE access | yes |
| 012 | 8fc1c96ae0 | 2025-12-09 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 24 | 158 | Allow custom CA certificate path for S3 connections — **PORTED `patch-port(012)` (`dfff5e80905`), `still-needed-but-rewrite`** (conflict-resolved against throttler/`try`-block drift). This commit also **fixes two bugs carried by the original patch**: Bug A — `EndpointPoolKey` omitted the SSL context, so the HTTP connection pool could hand back a connection validated for a *different* trust anchor (trust-isolation hole; now keyed by context pointer); Bug B — the `Poco::Net::Context` was rebuilt on every request/redirect (now built once and cached). Ships the uplift's first integration test (`test_aiven_s3_custom_ca_path` — inversion design, runtime-generated certs). See dossier [`012-s3-custom-ca-path.md`](../../patches/012-s3-custom-ca-path.md) and retrospective [`15-patch-012-retrospective.md`](15-patch-012-retrospective.md). | no |
| 013 | 2f70d49490 | 2025-12-09 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 2 | 8 | Allow custom CA certificate path for Azure Blob Storage connections — **PORTED `patch-port(013)` (`66d83382ed8`), `still-needed-but-rewrite`**. The original 25.8 mechanism (Azure SDK Curl transport, `curl_options.CAInfo`) is obsolete on 26.3: upstream migrated Azure HTTP to the Poco-based `PocoAzureHTTPClient`, so the field no longer exists. Rewritten as a mirror of patch 012 on the shared `makeHTTPSession` / `HTTPConnectionPool` / `Poco::Net::Context` machinery — `<ca_path>` → `RequestSettings::ca_path` → `PocoAzureHTTPClientConfiguration` → `makeCAContext` (file-local `static` to avoid an ODR clash with S3's `makeCAContext`). Trust isolation (Bug A) is inherited from 012's context-keyed pool; depends on 012 being present. Ships a proportionate integration test (`test_aiven_azure_custom_ca_path`) against HTTP Azurite (valid `ca_path` accepted; bogus `ca_path` fails with `File not found`), with end-to-end TLS-verification evidence inherited from 012. See dossier [`013-azure-custom-ca-path.md`](../../patches/013-azure-custom-ca-path.md). | no |
| 014 | e65f68836b | 2025-12-10 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 7 | 166 | Fix default profile escape vulnerability — **PORTED `patch-port(014)` (`90c53a46d29`), `still-needed-and-applies-clean`** (clean `git apply`, zero conflicts; all anchors present on 26.3). Closes a settings-profile privilege escalation: the opt-in `allow_non_default_profile` setting (`Bool`, default `true` = guard off) gates enforcement in `SettingsConstraints::check` — the shared sink for `CREATE/ALTER PROFILE`, `… USER … SETTINGS PROFILE`, and `… ROLE …`, so the user-creation vector is covered, not just profile creation — requiring every SQL-assigned profile to be the configured `default` or a descendant. Adds an **unconditional** circular-inheritance reject and auto-injection of the `default` parent when the guard is on. Faithful carry + 2 style cleanups (stray blank line, `and`→`&&`). Documented follow-up (§3.C): `isExpectedProfileOrDescendantLocked` walks the parent chain by unbounded recursion → stack-overflow while holding `SettingsProfilesCache::mutex` on a pre-existing cyclic graph (from `users.xml` / mixed-version replication, which bypass the SQL cycle check) — shipped faithful, hardening tracked. Ships stateless test `09078_allow_non_default_profile_escape` (escape-blocked-vs-opt-out causation pair). See dossier [`014-default-profile-escape.md`](../../patches/014-default-profile-escape.md). | yes |
| 015 | d3b5e9016f | 2025-12-12 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 17 | 297 | Allow delegating S3 signature to a separate process | no |
| 016 | 38e54d3589 | 2025-12-13 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 10 | 148 | Allow delegating Azure signature to a separate process | no |
| 017 | 0a8c8c86bd | 2025-12-13 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 1 | 12 | ~~Enforce SSL in the MySQL handler~~ **DROPPED — `obsoleted-by-upstream`. Upstream 26.3 ships the `mysql_require_secure_transport` server setting (default false; in base `v26.3.10.62-lts`, absent on 25.8) — when on, `MySQLHandler` refuses non-SSL connections at `MySQLHandler.cpp:282` (`SSL connection required.`), already exercised by `test_mysql_protocol::test_mysql_client_secure`. The Aiven patch hardcoded the same rejection in `finishHandshake` only because 25.8 had no such setting. Replacement is config, not code: set `mysql_require_secure_transport=true` (and `postgresql_require_secure_transport=true`) in Aiven's managed config — the security posture moves code→config (§5 handover). See dossier [`017-enforce-ssl-mysql-handler.md`](../../patches/017-enforce-ssl-mysql-handler.md).** | yes |
| 018 | ae35b0cc72 | 2025-12-13 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 3 | 32 | Enforce HTTPS for URL storage and HTTPDictionarySource — **PORTED `patch-port(018)` (`f7093e7c606`), `still-needed-but-rewrite`** (config-gated server setting `enforce_https_for_url_storage`, default `false` + non-session-overridable; enforcement extended to `StorageURLCluster` as a 4th file beyond the original 3). Ships integration test `test_aiven_enforce_https_url` (pre-patch 3 failed / post-patch 5 passed). See dossier [`018-enforce-https-url-storage.md`](../../patches/018-enforce-https-url-storage.md). | no |
| 019 | 05d8148a57 | 2025-12-13 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 14 | 287 | Allow avnadmin creating database using sql — **PORTED `patch-port(019)` (`2a8529c5935`), `still-needed-but-rewrite`** (same mechanism as source: 3 default-`""` server settings + cloned-context privilege elevation + curated `GRANT DEFAULT REPLICATED DATABASE PRIVILEGES`. Fix-forwards: grantee/`shard_macros` quoting (A/B), default-off `DROP` no-op when `cluster_database` empty (D); 26.3 drift fix: fresh `query_id` for nested internal queries at two sites — `createReplicatedDatabaseByClient` + `InterpreterGrantQuery` — else `Code 216`. C/E carried as intended). Ships integration test `test_aiven_indirect_database_creation` (11 cases, 11/11 pass incl. non-escalation hard gate `test_f`). See dossier [`019-avnadmin-indirect-database-creation.md`](../../patches/019-avnadmin-indirect-database-creation.md). | no |
| 020 | c5b03b2c0e | 2025-12-13 | tilman.moeller@aiven.io | alex.khatskevich@aiven.io | 1 | 1 | Add CHECK TABLE to default privileges — **PORTED `patch-port(020)` (`507fce53586`), `still-needed`** (one-line: adds `CHECK` to the `GRANT DEFAULT REPLICATED DATABASE PRIVILEGES` set; applied as a direct edit, not a cherry-pick, since the nearby `backQuote(grantee)` line diverges from source. `CHECK` confirmed still separately grantable on 26.3 — `AccessType.h:162`). Tail of the `019→020` chain. Validated via 019 case C (asserts `CHECK`); full module 11/11. See dossier [`020-check-table-default-privileges.md`](../../patches/020-check-table-default-privileges.md). | no |
| 021 | 934b35cc7d | 2025-12-15 | tilman.moeller@aiven.io | joelynch112@gmail.com | 28 | 236 | Add SSL/TLS configuration support for PostgreSQL and MySQL connections — **PORTED `patch-port(021)` (`58b8a259e4e`), `still-needed-but-rewrite`** (no upstream PG/MySQL client SSL on 26.3; apply-then-fix, 1 real conflict — kept the 26.3 `MySQLDictionarySource` non-named-collection `else` branch, `ssl_mode` threaded only via named collection. Faithful `Settings` carry (session-level `postgresql_connection_pool_ssl_mode`/`_ssl_root_cert` + `SSLMode`/`MySQLSSLMode` enums, default `PREFER`), not `ServerSettings`. Submodule fork-redirect (clause vi) adapted 25.8→26.3: `url=aiven/mariadb-connector-c`, `branch=aiven/clickhouse-v26.3.10.62`, gitlink `2914d3f` (`X509_check_host` hostname-length fix). `M(CLASS_NAME, SSLMode)` placed alphabetically in `Settings.h`. Connector macro `MYSQL_OPT_SSL_VERIFY_SERVER_CERT` verified present in the pin. Ships integration test `test_aiven_external_db_ssl` (decoupled Dispatch 2, §7.3): post-patch 10/10, pre-patch 5 CH-side cases fail on SSL-feature-absence (`UNRECOGNIZED_ARGUMENTS`/`BAD_ARGUMENTS`). X509 `verify_full` hostname-mismatch exercised post-patch; full connector pre/post flip out of scope. See dossier [`021-external-db-ssl.md`](../../patches/021-external-db-ssl.md). | no |
| 022 | ce74bc008d (+ ON CLUSTER fix `f4552b14c77`) | 2025-12-15 | tilman.moeller@aiven.io | joelynch112@gmail.com | 38 | 847 | Added support for protected users — **PORTED `patch-port(022)`, `still-needed-but-rewrite`.** Combines base `ce74bc008d` with the follow-up security fix `f4552b14c77` ("Enforce protected-user checks before ON CLUSTER dispatch") as a single commit. Adds a `Protected` flag on users, the `PROTECTED_ACCESS_MANAGEMENT` privilege, parser support for `PROTECTED` / `NOT PROTECTED`, a `CheckFunc` threaded through `IAccessStorage` write paths (incl. the ZooKeeper-replicated storage), and fleet-wide self-drop/self-revoke prevention. The Access subsystem drifted 25.8→26.3 (storage signatures, `DiskAccessStorage` now delegates to an in-memory storage, `ZooKeeperReplicator` gained `setCurrentComponent` guards + 1000-attempt retries, `MemoryAccessStorage` gained a `notify` param, `InterpreterDropAccessEntityQuery` gained a `MASKING_POLICY` branch) — 9 files conflicted and were adapted to current signatures rather than force-applied; the 3 interpreters rewritten by the fix were reconstructed in their final post-fix form. Dropped the base's debug-only `Context.cpp` logging and the emoji/`LOG_INFO` noise in `ZooKeeperReplicator`. Security invariant verified: the self-protection + `isProtected`→`PROTECTED_ACCESS_MANAGEMENT` checks run on the initiator BEFORE `executeDDLQueryOnCluster`, and the `CheckFunc` fires on every storage write path including the ZK-replicated one. Ships stateless tests `09079_protected_user_extra_statements` (ported from base 04004) + `09080_protected_user_management` (core matrix), and integration test `test_aiven_protected_users` (2 nodes + Keeper, `ReplicatedAccessStorage`; asserts ON CLUSTER bypass denied on initiator + `Protected` flag replicates via ZooKeeper). See dossier [`022-protected-users.md`](../../patches/022-protected-users.md). | no |
| 023 | abd678347b | 2025-12-15 | tilman.moeller@aiven.io | joelynch112@gmail.com | 1 | 2 | **ALREADY-APPLIED (folded into `patch-port(015)` `5da90310af3`)** — pure `contrib/aws` gitlink bump; the 26.3 `aiven/aws-sdk-cpp` branch stacks 015→023, and 015's port pinned the gitlink to the branch HEAD `c930cb8e8c51` which IS the "Fix IPv6 S3 object storage host" commit. No separate commit/build/test (an empty no-op bump). See dossier [`023-fix-ipv6-s3-object-storage-host.md`](../../patches/023-fix-ipv6-s3-object-storage-host.md). — Fix IPv6 S3 object storage host The AWS SDK does not fully support IPv6 in hostnames, mainly because the escaping brackets are not parsed and removed at the right time. | yes |
| 024 | 1abdc712c0 | 2025-12-16 | tilman.moeller@aiven.io | joelynch112@gmail.com | 2 | 4 | **PORTED `patch-port(024)` (`0df4e5bd2c8`), `still-needed` (source leg).** Submodule (`contrib/azure`) leg already folded into `patch-port(016)` (azure HEAD `98519bd3` "Improve IPv6 support"); only the `validateStorageAccountUrl` regex (broadened for bracketed IPv6 + optional port) is ported, as a 1-line direct edit at the relocated path `src/Disks/DiskObjectStorage/ObjectStorages/AzureBlobStorage/AzureBlobStorageCommon.cpp`. Stateless test `9024_ipv6_azure_storage_account_url` (worktree-flip evidence pair: post-patch PASS, pre-patch FAIL). Dossier [`024-fix-ipv6-azure-object-storage-host.md`](../../patches/024-fix-ipv6-azure-object-storage-host.md). — Fix IPv6 Azure object storage host | yes (9024) |
| 025 | fdb7a142d1 | 2025-12-16 | tilman.moeller@aiven.io | joelynch112@gmail.com | 1 | 3 | **PORTED `patch-port(025)` (`171a7fb1be1`), `still-needed`.** Adds opt-in `storage_prefix` Azure disk option (prepended to every blob key) as a 3-line direct edit in `processEndpoint` at the relocated path `src/Disks/DiskObjectStorage/ObjectStorages/AzureBlobStorage/AzureBlobStorageCommon.cpp`. Azurite integration test `test_aiven_azure_storage_prefix` (worktree-flip evidence pair: post-patch 2 passed; pre-patch the prefix-applied test fails with keys at container root). Dossier [`025-azure-storage-prefix.md`](../../patches/025-azure-storage-prefix.md). — Add support for Azure object storage path prefix | yes (test_aiven_azure_storage_prefix) |
| 026 | 3139011de0 | 2025-12-16 | tilman.moeller@aiven.io | joelynch112@gmail.com | 14 | 343 | **PORTED `patch-port(026)`, `still-needed-but-rewrite` (COMMITTED `fd8bdaac976`).** Lean rewrite against the 26.3 `DiskObjectStorage` redesign (single `object_storage` → `ObjectStorageRouter` multi-location registry + disk-level `wrapped_disk` layering). The `backup` disk wraps an object-storage disk with a `BackupObjectStorage` decorator that **soft-deletes** blobs (writes a local deletion-marker instead of physical `unlink`; external GC decides physical removal), reconciled to 26.3's expanded `IObjectStorage` pure-virtual set. **Scoped to single-location object-storage disks** (Aiven tiered storage); the multi-location/replicated case is rejected with an explicit `BAD_ARGUMENTS` guard in `wrapWithBackup` (the soft-delete contract is unsound when background replication/GC can't observe the markers). Adds `supportsLayers` to `IDisk`/`DiskObjectStorage` (mid-class virtual → required a forced `IDisk.h`-closure rebuild, 865 TUs, vtable-consistent binary); flips the `MergeTreeData` layer-enumeration gate `supportsCache`→`supportsLayers` and walks the delegate disk's layers (delivered as `tmp/patch-026/mergetreedata-gateflip.patch` due to a `spawn E2BIG` tooling limit on the 454 KB file). **Soft-delete defect found and fixed (Fix A):** the inner/wrapped disk's `BlobKillerThread` shared the one per-`metadata_storage` deferred-removal queue and physically `unlink`ed blobs, defeating the markers; fixed by making the backup disk's killer the **sole drainer** (`detachWrapped` + `disable` the inner killer). Ships integration test `test_aiven_backup_disk` proving **physical blob survival across all four removal paths — DROP, merge, mutation, TTL-delete** — with a Fix-A-isolating evidence pair (post-fix 4/4 PASS; pre-fix 4/4 FAIL on exactly the physical-survival assertion). See dossier [`026-backup-disk-type.md`](../../patches/026-backup-disk-type.md). — Add Backup disk type | no |
| 027 | 2f052691db | 2025-12-18 | tilman.moeller@aiven.io | joelynch112@gmail.com | 1 | 11 | **DROPPED — `obsoleted-by-upstream`.** 26.3 redesigned the `WriteBuffer` lifecycle: the base `~WriteBuffer` no longer finalizes in the destructor (only `chassert`s when `!isStackUnwinding()`), and `~WriteBufferFromS3` best-effort aborts the partial upload (`tryToAbortMultipartUpload`). So the throwing-finalize-in-destructor crash cannot occur; a faithful port (`out->finalize()` in `catch`) would instead COMMIT partial/corrupt data (the 26.3 error-path primitive is `cancel`). No code/commit/test. Dossier [`027-fix-uncaught-exception-s3-storage.md`](../../patches/027-fix-uncaught-exception-s3-storage.md). — Fix uncaught exception if S3 storage fails | n/a |
| 028 | 8ed6167986 | 2025-12-18 | tilman.moeller@aiven.io | joelynch112@gmail.com | 1 | 1 | **DROPPED — `obsoleted-by-upstream`.** The patch's restart-resilience intent (and the restart/attach leg of its perf claim) is already provided by upstream `7e7cbbdd224` ("Do not make requests to Azure on server restart"): `createObjectStorage` gets `is_readonly = args.mode != LoadingStrictnessLevel::CREATE`, and `getContainerClient`'s `… || readonly` short-circuit returns the non-networking `createForContainer()` on restart/attach (no `containerExists`/`CreateBlobContainer` call). Upstream also ships `tests/integration/test_restart_with_unavailable_azure`. 028's only residual effect on 26.3 would be on the `readonly=false` (CREATE) path: setting `container_already_exists = true` unconditionally suppresses container auto-creation on `CREATE TABLE` — an unrelated behavior change / mild regression for general users. No code/commit/test. Dossier [`028-skip-create-azure-container.md`](../../patches/028-skip-create-azure-container.md). — Skip attempt to create a container in azure blob storage | n/a |
| 029 | f45b8fb6fb | 2025-12-22 | tilman.moeller@aiven.io | joelynch112@gmail.com | 8 | 263 | **PORTED `patch-port(029)`, `still-needed-but-rewrite` (COMMITTED `dd883f767f9`).** Faithful **enum** port of the Aiven Kafka SASL/SSL settings — explicitly **NOT** obsoleted-by-upstream. Upstream 26.3 ships `kafka_security_protocol`/`kafka_sasl_mechanism` only as plain `String` (and lacks `kafka_ssl_endpoint_identification_algorithm` + the three `ssl_*_location` settings entirely); a `String` is not behavior-equivalent to the enum contract that **both 25.3 and 25.8 shipped** — it loses DDL-time validation, hyphen→underscore normalization (`SASL-SSL`→`SASL_SSL`), and the always-applied `endpoint=none` default (librdkafka defaults to `https`, `rdkafka_conf.c:890`; librdkafka matches these enums case-insensitively, `rdkafka_conf.c:1890`). Re-flips the two settings `String→enum` and adds the four SSL settings (`endpoint` enum default `NONE` + 3 `String` locations); enum traits carried verbatim from `f45b8fb6fb`. Two adaptations: (1) **direct-settings wiring** in `updateGlobalConfiguration` (reads `storage.getKafkaSettings()` directly), so `StorageKafka.cpp`/`StorageKafka2.cpp`/`KafkaConfigLoader.h` are **untouched** vs the original's params-struct plumbing; (2) **25.8 logic** — `ssl.ca/certificate/key.location` conditional on non-empty, security/sasl/endpoint always-set. `endpoint=none` default preserved deliberately (hostname verification off by default — matches 25.3/25.8). Type-flip blast radius self-contained (only `KafkaConfigLoader.cpp` reads these). Full `clickhouse` build exit 0. Stateless test `9029_kafka_sasl_ssl_settings` (canonical + case/hyphen acceptance + invalid-value `BAD_ARGUMENTS` rejection); behaviors verified against the patched binary via `clickhouse local` (harness server start blocked by an unrelated 26.3 `DiskSelector`/`database_disk` config quirk). See dossier [`029-kafka-sasl-ssl-settings.md`](../../patches/029-kafka-sasl-ssl-settings.md). — Add Kafka configuration support for SASL and SSL settings | yes (9029) |
| 030 | 37b61f443b | 2025-12-22 | tilman.moeller@aiven.io | joelynch112@gmail.com | 3 | 10 | **PORTED `patch-port(030)`, `still-needed` (COMMITTED `94dd391d108`).** Clean cherry-pick (applied via `git apply` of `format-patch`, since `git cherry-pick -n` was blocked by a repo hook; functionally identical), all three landmarks present and unchanged on 26.3, `git apply --check` exits 0. Removes the `num_consumers < 1` DDL validation in `StorageKafkaUtils.cpp` so `kafka_num_consumers = 0` globally disables Kafka consumption (opt-in ops lever, no default change, narrow blast radius — clause v), and guards division-by-zero in `StorageKafka::getMaxBlockSize` / `StorageKafka2::getMaxBlockSize` via a `nonzero_num_consumers` value of 1. The `StorageKafka2` guard was already part of the 25.8 source commit, so there is no third-engine gap. Stateless test `9030_kafka_num_consumers_zero` (worktree-flip evidence pair: post-patch PASS; pre-patch FAIL with `Number of consumers can not be lower than 1` / `BAD_ARGUMENTS` at `StorageKafkaUtils.cpp:211`); div-by-zero guards covered by inspection. See dossier [`030-kafka-num-consumers-zero.md`](../../patches/030-kafka-num-consumers-zero.md). — Allow decreasing number of Kafka consumers to zero | yes (9030) |
| 031 | 7d48793fc5 | 2025-12-22 | tilman.moeller@aiven.io | joelynch112@gmail.com | 5 | 30 | **PORTED `patch-port(031)`, `partial` (COMMITTED `537d05d7349`).** Three legs, mostly overtaken by upstream. **Leg A (per-table `kafka_format_avro_schema_registry_url`): `obsoleted-by-upstream`** — 26.3 folds `LIST_OF_ALL_FORMAT_SETTINGS` into `KafkaSettings` (`KafkaSettings.cpp:70`) and `getFormatSettings` forwards every non-`kafka_`-prefixed setting (`StorageKafkaUtils.cpp:524-525`), so the canonical `format_avro_schema_registry_url` already works per-table. **Re-added as a thin backward-compat alias** (maintainer decision: 25.3/25.8 tables carry the `kafka_`-prefixed name, which would otherwise fail `UNKNOWN_SETTING` on `ATTACH`) — `DECLARE` in `KafkaSettings.cpp` + explicit forward in `createSettingsAdjustments` (placed after the generic passthrough so the alias wins); the original's vestigial write-only `StorageKafka` member is dropped (no dead code). **Leg B (schema-registry basic auth): `obsoleted-by-upstream` and upstream is superior** — HEAD `AvroRowInputFormat.cpp:1109-1127` already parses `user:pass@` and additionally URL-decodes the credentials (`HTTPCredentials::fromUserInfo`), which the patch's raw `find(':')` did not; dropped (porting would regress). **Leg C (credential-stripped logging): `still-needed`** — HEAD still logs the full registry URL incl. credentials at `LOG_TRACE` (`AvroRowInputFormat.cpp:1095`); carried the `setUserInfo("")` sanitization. Build exit 0. Stateless test `9031_kafka_schema_registry_url_alias` (alias accepted at DDL; canonical name also verified via `clickhouse local`); Leg C covered by inspection. See dossier [`031-kafka-schema-registry-auth.md`](../../patches/031-kafka-schema-registry-auth.md). — Support per-table schema registry with authentication | yes (9031) |
| 032 | 9f769ed901 | 2025-12-23 | tilman.moeller@aiven.io | joelynch112@gmail.com | 9 | 38 | **PORTED `patch-port(032)`, `partial` (COMMITTED `bed2c87dc84`).** Two independent legs. **Leg 1 (`kafka_auto_offset_reset`): `still-needed`, faithful enum port** — HEAD hardcodes `conf.set("auto.offset.reset", "earliest")` at `KafkaConfigLoader.cpp:532` with no per-table override and no upstream equivalent; both 25.3 and 25.8 shipped the `KafkaAutoOffsetReset` enum (values `smallest`/`earliest`/`beginning`/`largest`/`latest`/`end`, default `EARLIEST`). Ported as an enum (same rationale as 029: DDL-time validation + case-insensitivity + stable stored-DDL contract a `String` lacks); default `EARLIEST` preserves the hardcoded behavior byte-for-byte. Rewritten the 26.3 way with **direct-settings wiring** — `conf.set("auto.offset.reset", SettingFieldKafkaAutoOffsetResetTraits::toString(storage.getKafkaSettings()[KafkaSetting::kafka_auto_offset_reset].value))` inside the templated `getConsumerConfiguration` (mirrors 029/033) — so **`KafkaConfigLoader.h`/`StorageKafka.cpp`/`StorageKafka2.cpp` are untouched** (no `ConsumerConfigParams` struct change vs the original's params-struct plumbing). **Leg 2 (`kafka_date_time_input_format`): `obsoleted-by-upstream`** — 26.3's generic format passthrough (`getFormatSettings` → `createSettingsAdjustments`, `StorageKafkaUtils.cpp:525-526`) already forwards the canonical `date_time_input_format` set directly on a `Kafka` table. Re-added as a thin backward-compat alias (25.3/25.8 DDL carries the `kafka_`-prefixed name, else `UNKNOWN_SETTING` on `ATTACH`) — mirrors the 031 schema-registry shim. The alias forward is **`.changed`-guarded** (intentional divergence from the original's unconditional emit): `SettingsChanges` is a last-wins `std::vector` applied in order by `Context::applySettingsChangesWithLock` with no dedup, so the guarded alias emitted after the generic passthrough yields canonical-only→canonical, alias-only→alias, both→alias-wins without clobbering a user's canonical value. `DateTimeInputFormat` was already in the `KafkaSettings.h` `M(CLASS_NAME, …)` list (no enum-list edit). Build exit 0. Stateless test `9032_kafka_offset_reset_datetime` (offset-reset canonical acceptance + case-sensitivity — the standard `IMPLEMENT_SETTING_ENUM` macro is case-sensitive, so `'LATEST'` is rejected with `BAD_ARGUMENTS`, matching the original 25.8; the dispatch spec's case-insensitivity assumption was empirically false — + invalid-value `BAD_ARGUMENTS` rejection; datetime alias + canonical accept) verified against the patched binary via `clickhouse local` (harness server start blocked by an unrelated 26.3 `DiskSelector`/`database_disk` config quirk); runtime `auto.offset.reset`→librdkafka wiring and alias precedence covered by inspection. Patch 076 (`kafka_auto_offset_reset_ms`) extends Leg 1, so 032 lands first. See dossier [`032-kafka-offset-reset-datetime.md`](../../patches/032-kafka-offset-reset-datetime.md). — Add kafka_auto_offset_reset and kafka_date_time_input_format settings | yes (9032) |
| 033 | 6384eccfac | 2025-12-23 | tilman.moeller@aiven.io | joelynch112@gmail.com | 8 | 95 | **PORTED `patch-port(033)`, `partial` (COMMITTED `bf01b1a87df`).** Two legs. **Leg 1 (six `kafka_producer_*` throughput knobs): `still-needed`** — HEAD ships none of `kafka_producer_batch_size`/`batch_num_messages`/`linger_ms`/`queue_buffering_max_messages`/`queue_buffering_max_kbytes`/`request_required_acks`, and `getProducerConfiguration` sets none of the corresponding librdkafka producer properties; there is no per-table producer-batching/buffering tuning on 26.3. Carried, **rewritten the 26.3 way**: applied via a direct `storage.getKafkaSettings()` read inside the templated `KafkaConfigLoader::getProducerConfiguration` (mirrors patch 029's direct-settings wiring), so **`StorageKafka.cpp`/`StorageKafka2.cpp`/`KafkaConfigLoader.h` are untouched** (no `ProducerConfigParams` struct change). The 25.8 `0`-skip guard in `setKafkaConfigValue` is carried — librdkafka's minimum for the five batching/buffering properties (`batch.size`, `batch.num.messages`, `linger.ms`, `queue.buffering.max.messages`, `queue.buffering.max.kbytes`) is `>= 1`, so a literal `"0"` (meaning "use librdkafka default") must not be forwarded; `request.required.acks` is excluded from the guard (`-1`/`0`/`1` all valid). Placed after `kafka_ssl_key_location` (not after `kafka_auto_offset_reset`, which is patch 032 and not yet present) — **no 032 dependency**. **Leg 2 (compression): `obsoleted-by-upstream`** — HEAD already ships `kafka_compression_codec` (`String`, `KafkaSettings.cpp:57`) + `kafka_compression_level` (`Int64`, line 58) wired at `KafkaConfigLoader.cpp:408-412`; the patch's `KafkaCompressionCodec` **enum is dropped** (upstream codec is a `String`; `SettingsEnums.{h,cpp}` and the `KafkaSettings.h` `M(CLASS_NAME, …)` list are untouched). Kept as `DECLARE_WITH_ALIAS`/`ALIAS` backward-compat aliases so 25.3/25.8 DDL carrying `kafka_producer_compression_codec`/`kafka_producer_compression_level` still parses (else `UNKNOWN_SETTING` on `ATTACH`). Build exit 0. Stateless test `9033_kafka_producer_settings` (all six knobs accepted at DDL; alias names accepted; canonical upstream names accepted) verified against the patched binary via `clickhouse local` (harness server start blocked by an unrelated 26.3 `DiskSelector`/`database_disk` config quirk); the `0`-skip guard covered by inspection. See dossier [`033-kafka-extra-settings.md`](../../patches/033-kafka-extra-settings.md). — Add extra settings to Kafka Table Engine | yes (9033) |
| 034 | b1a99ca92b | 2026-01-02 | tilman.moeller@aiven.io | joelynch112@gmail.com | 2 | 322 | **PORTED `patch-port(034)`, `still-needed-but-rewrite` (COMMITTED `90aa42cf6a6`).** Faithful full refactor of `DatabasePostgreSQL` to reduce the per-database `mutex` scope so **no PostgreSQL network call runs while the lock is held** — a failing/slow PostgreSQL integration must not wedge `system.databases` reads (the `comment`/`database_name` are read under the same base mutex via `getDatabaseComment`/`getCreateDatabaseQuery`). Original does **not** apply (conflict at `DatabasePostgreSQL.h:87`): the 26.3 file drifted and gained clang thread-safety annotations absent on 25.8 (`database_name` is now `TSA_GUARDED_BY(mutex)`; member `fetchTable`/`getCreateDatabaseQueryImpl` carry `TSA_REQUIRES(mutex)`). Every method now copies the plain values it needs under a brief lock, releases, does the PostgreSQL I/O off-lock, and re-acquires + re-validates to mutate `cached_tables`/`detached_or_dropped` (`empty`/`getTablesIterator`/`isTableExist`/`tryGetTable`/`attachTable`/`detachTable`/`dropTable`/`removeOutdatedTables`/`getCreateTableQueryImpl`); the four helpers (`getTableNameForLogs`/`formatTableName`/`checkPostgresTable`/`fetchTable`) are now free functions in an anonymous namespace. **TSA reconciliation:** removed the member helper decls (incl. `fetchTable … TSA_REQUIRES(mutex)`), kept `getCreateDatabaseQueryImpl … TSA_REQUIRES(mutex)` (no PG I/O) — builds clean under `-Weverything`/`-Wthread-safety` + `-Werror`, **no** `TSA_NO_THREAD_SAFETY_ANALYSIS`. **Deviations from source (§3):** (1) helpers in anon namespace (source left some external-linkage); (2) **`removeOutdatedTables` fetch moved OFF the lock** — the source kept the re-acquired lock around the connect, which contradicts the ratified invariant; only the reconciliation runs under the re-acquired lock; (3) `getTablesIterator` cleanup loop preserved verbatim (suspicious-looking but output-correct); (4) `make_intrusive` drift kept; (5) `detachTable` PG-existence-before-dropped check order kept as in source. **26.3 DROP-path drift:** `DROP DATABASE` for `PostgreSQL` skips table iteration (`shouldBeEmptyOnDetach == false`), so it does no PG I/O; pre-patch it still couples to a dead backend only via `shutdown`→`cleaner_task->deactivate` waiting for the lock-held cleaner connect. Build exit 0. Integration test `test_aiven_postgres_unlock` against a **blackhole** PG host (`10.255.255.1:5432`, dropped SYN) with a worktree-flip evidence pair: post-patch 2/2 pass (`system.databases` read ~ms, `DROP` <1s, bound 3s); pre-patch 2/2 fail on the latency assertions (`system.databases` **11.91s**, `DROP` **5.99s** vs 3s bound). The deterministic flip is the `system.databases` responsiveness test; the DROP-promptness flip is timing-dependent (via the cleaner). See dossier [`034-unlock-postgresql-database.md`](../../patches/034-unlock-postgresql-database.md). — Unlock PostgreSQL database | yes (test_aiven_postgres_unlock) |
| 035 | 65248bdd34 | 2026-01-02 | tilman.moeller@aiven.io | joelynch112@gmail.com | 1 | 5 | Add support for integration metadata to named collections validation | yes |
| 036 | d293fbe109 | 2026-01-02 | tilman.moeller@aiven.io | joelynch112@gmail.com | 3 | 157 | Multiple changes in PostgreSQL dictionary | no |
| 037 | 7a7058eba8 | 2026-01-05 | tilman.moeller@aiven.io | joelynch112@gmail.com | 1 | 2 | Ignore unreadable sensors — **PORTED `patch-port(037)`, `test_design_blocked`** (untestable: the suppressed `LOG_WARNING` only fires on broken hwmon/EDAC sensors that CI lacks, and `/sys` paths can't be fault-injected; human-approved no-test exception). Clean cherry-pick, patch-id-identical. See dossier [`037-ignore-unreadable-sensors.md`](../../patches/037-ignore-unreadable-sensors.md). | yes |
| 038 | a498627944 | 2026-01-05 | tilman.moeller@aiven.io | joelynch112@gmail.com | 2 | 25 | Fix tcp_port_secure from ZK — **COMMITTED `patch-port(038)` (`e9c0e2d1d00`)**, `still-needed-and-applies`. Routes `getTCPPortSecure` through a new non-throwing `tryGetServerPort` (reads the bound port from `server_ports` instead of static config, for Aiven's ZK/dynamic-port deployment), write-locks `registerServerPort`, and changes the unknown-port error `CLUSTER_DOESNT_EXIST`→`BAD_GET`. Cherry-pick auto-merged clean; `byte_equivalent: false` but `decomposition-strict` empty (only line-number drift + an upstream `static_cast<UInt16>` on the neighboring `getTCPPort` context line). Test = stateless `9038_fix_tcp_port_secure_from_zk` gating on the SQL-observable error-code transition via `getServerPort()`; evidence pair: pre-patch FAIL (`CLUSTER_DOESNT_EXIST`/701), post-patch PASS (`BAD_GET`/170). Headline ZK/dynamic-port path not stock-reproducible (see dossier §4). Dossier: `docs/aiven/patches/038-fix-tcp-port-secure-from-zk.md`. | yes (9038) |
| 039 | 32e9abc159 | 2026-01-05 | tilman.moeller@aiven.io | joelynch112@gmail.com | 1 | 8 | Disable thread fuzzer — **PORTED `patch-port(039)`, `no_justified`** (compile-time toggle of test-only `ThreadFuzzer` pthread wrapping; no runtime/SQL surface). Causation verified at artifact level: `nm` shows `pthread_mutex_lock`/`_unlock` interposers + 16 tuning statics present pre-patch, absent post-patch; `U __pthread_mutex_lock@GLIBC_2.17` corroborates the glibc-2.36+ rationale. Clean cherry-pick, patch-id-identical, full build exit 0. See dossier [`039-disable-thread-fuzzer.md`](../../patches/039-disable-thread-fuzzer.md). | yes |
| 040 | 1151af44bb | 2026-01-05 | tilman.moeller@aiven.io | joelynch112@gmail.com | 1 | 6 | Disable replicas_status endpoint | yes |
| 041 | a05665ce57 | 2026-01-05 | tilman.moeller@aiven.io | joelynch112@gmail.com | 7 | 138 | Fix swap drift — **COMMITTED `patch-port(041)` (`529f1128e9f`)**, `still-needed-but-rewrite`. Folds swapped bytes into the `MemoryTracker` (RSS+swap) read from `/proc/self/status` (not `/proc/self/statm`); exposes a `MemorySwap` async metric. One `MemoryWorker.cpp` conflict resolved (upstream `try`/`catch` + `USE_JEMALLOC` drift); the source's K&R brace in `AsynchronousMetrics.cpp` reduced to Allman. `byte_equivalent: false`. Test = stateless `9041_swap_drift` (asserts `MemorySwap` present); post-patch PASS — shipped with the documented, mechanically-certain pre-patch differential (FAIL not empirically run). Dossier: `docs/aiven/patches/041-swap-drift.md`. | no |
| 042 | 0d6eb5bece | 2026-01-05 | tilman.moeller@aiven.io | joelynch112@gmail.com | 2 | 18 | Fix ZK node leak after create delete table | yes |
| 043 | 6b54a78936 | 2026-01-05 | tilman.moeller@aiven.io | joelynch112@gmail.com | 1 | 2 | ~~Remove SSBS check from `NO_ARMV81_OR_HIGHER`~~ **DROPPED — `superseded-by-upstream-equivalent`. Upstream commit `545c27008d4` (Konstantin Bogdanov, 2025-10-03) makes the identical 1-line change and is in `v26.3.10.62-lts` (not in 25.8), so HEAD already equals the patch's post-image — applying it is a no-op. See dossier [`043-remove-ssbs-check-from-no-armv81-or-higher.md`](../../patches/043-remove-ssbs-check-from-no-armv81-or-higher.md).** | no |
| 044 | b8e3cdd8ea | 2026-01-05 | tilman.moeller@aiven.io | joelynch112@gmail.com | 1 | 2 | ~~Enable curl ipv6~~ **DROPPED — `ineffective-no-op`. `curl_config.h` already `#define ENABLE_IPV6` (upstream `9251fb803a`, 2020, in both LTS bases) and `curl_setup.h` maps it to `USE_IPV6`, so IPv6 is already on. The patch's `option(ENABLE_IPV6)` is never read and its compile-define is unconditional+redundant, so it does NOT make IPv6 disableable (its stated purpose) — a no-op on both LTSes. See dossier [`044-enable-curl-ipv6.md`](../../patches/044-enable-curl-ipv6.md).** | yes |
| 045 | d4973afdd7 | 2026-01-06 | tilman.moeller@aiven.io | joelynch112@gmail.com | 3 | 45 | Allow disabling of individual dictionary sources | no |
| 046 | 7e08e44e51 | 2026-01-06 | tilman.moeller@aiven.io | joelynch112@gmail.com | 9 | 93 | Add early fetch pool — **PORTED `patch-port(046)`, conflict-resolved + drift-fixed + RENAMED (`aiven_` convention).** Splits the `GET_PART`/`ATTACH_PART` fetch path so empty-`source_replica` initial-sync fetches use a separate "early" pool instead of starving the normal fetch pool. Settings renamed: `use_early_fetch_pool` → `aiven_use_early_fetch_pool` (MergeTreeSetting, Bool, default **true**, `DECLARE_WITH_ALIAS` keeps the old name for metadata compat) and `background_early_fetches_pool_size` → `aiven_background_early_fetches_pool_size` (ServerSetting, UInt64, default 8, plain rename — config update required). Internal symbols/metrics (`early_fetch_executor`, `BackgroundEarlyFetchesPool*`, `"EarlyFetch"`) unprefixed. Drift fixes: `OrdinaryBackgroundExecutor` now takes a `ThreadName` enum (added `MERGETREE_EARLY_FETCH`→`"EarlyFetch"`), `.empty()` vs `== ""`, `canExecuteFetch` re-anchored to wrap only the saturation check. PC-GATE: default kept `true` (test-neutral; registered in `SettingsChangesHistory.cpp` for the 02995 golden list). Ships integration test `test_aiven_early_fetch_pool` (two-table routing differential via `system.replication_queue.postpone_reason` "early fetches already executing"). See dossier [`046-early-fetch-pool.md`](../../patches/046-early-fetch-pool.md). | no |
| 047 | 4a05c78da7 | 2026-01-06 | tilman.moeller@aiven.io | joelynch112@gmail.com | 5 | 62 | Add per-server override for max bytes to merge/mutate | no |
| 048 | c6d9323ba2 | 2026-01-07 | tilman.moeller@aiven.io | joelynch112@gmail.com | 2 | 28 | **PORTED `patch-port(048)`, `partial` (COMMITTED `72a22292ef5`).** Two independent legs. **Leg 1 (drop-table validation): DROPPED — `obsoleted-by-upstream`.** Upstream `25b0406c35c` (Alexey Milovidov, 2026-02-15, "Fix table drop getting permanently stuck with zero-copy replication"; ancestor of HEAD) reworked the exact `MergeTreeData::dropAllData` block: it no longer throws `ZERO_COPY_REPLICATION_ERROR` but `LOG_WARNING`s and calls `removeSharedRecursive(..., keep_all_shared_data=true, {})`. Porting the old patch would DELETE this shared-data-preserving branch and regress safety, so `MergeTreeData.cpp` is **untouched**. **Leg 2 (sharding zero-copy lock race): PORTED — `still-needed`.** Adds idempotent pre-creation (`createAncestors` + `createIfNotExists`) of the part-level zero-copy lock ancestor `.../zero_copy_<disk>/<uuid>/<part_name>` in `ReplicatedMergeTreeSink.cpp` before the commit ops are assembled; without it, two shards sharing a `table_shared_id` (same Replicated-DB table UUID) committing a same-named part race on the non-idempotent hard `CREATE` (`checkExistsAndGetCreateAncestorsOps`), and the loser hits `LOGICAL_ERROR` "It's a bug. No race is possible since it is a new part." (no upstream fix). Static `getZeroCopyPartPath` call qualified (`StorageReplicatedMergeTree::`) to keep clang-tidy clean. Ships integration test `test_aiven_zero_copy_lock_race` with a **deterministic** worktree-flip evidence pair (post-patch PASS; pre-patch FAIL on the exact `LOGICAL_ERROR`) made deterministic via the EXISTING `rmt_delay_commit_part` failpoint (no production failpoint added). See dossier [`048-zero-copy-fixes.md`](../../patches/048-zero-copy-fixes.md). — Zero copy fixes | no (test_aiven_zero_copy_lock_race) |
| 049 | cc745f53f9 | 2026-01-07 | tilman.moeller@aiven.io | joelynch112@gmail.com | 1 | 3 | Fix refreshable materialized views where there is a shard macro in the target table | yes |
| 050 | 63f05c4e12 | 2026-01-08 | tilman.moeller@aiven.io | joelynch112@gmail.com | 1 | 27 | Allow refreshable materialized views when using ZooKeeper (rather than ClickHouse Keeper) | yes |
| 051 | 4641e51fdc | 2026-01-08 | tilman.moeller@aiven.io | joelynch112@gmail.com | 5 | 81 | **COMMITTED — `patch-port(051)` (`46880f95584`).** Foundation of the `REGISTER_*` engine/function expose-gate family (sibling of committed 045; precursor of 052/070/071/075). De-escalated from clause-(v): ported as a no-default-change capability — all 29 flags default ON via `option(... ON)` in `src/configure_config.cmake` (the 26.3 adaptation; the 25.8 source omitted it and relied on the private build's `-D` flags). `byte_equivalent: false`. Test `no_justified` (build-system-only; default-ON ⇒ upstream behavior, exercised by the suite; mirrors 045). Verified: build green, generated `config.h` all 29 = 1, `system.table_functions`/`system.table_engines` smoke 6/6. Dossier [`051-disable-table-engines-and-functions.md`](../../patches/051-disable-table-engines-and-functions.md). — Added support for disabling various table engines and table functions | no |
| 052 | f987c06d9e | 2026-01-08 | tilman.moeller@aiven.io | joelynch112@gmail.com | 3 | 15 | **COMMITTED — `patch-port(052)` (`1b3cfe07bc6`).** Second link of the `REGISTER_*` engine/function expose-gate family (chains onto committed 051; precursor of 070/071/075). Adds 5 build-time flags (`REGISTER_TIMESERIES_TABLE_ENGINE`, `REGISTER_OBJECT_STORAGE_TABLE_ENGINE`, `REGISTER_TIMESERIES_FUNCTION`, `REGISTER_OBJECT_STORAGE_FUNCTION`, `REGISTER_DATALAKE_FUNCTION`), all default ON via `option(... ON)` in `src/configure_config.cmake` (the 26.3 adaptation; the 25.8 source omitted it). Two trivial cherry-pick context conflicts hand-resolved: `config.h.in` stale `SOURCE_DIR` trailer, and a new-in-26.3 `registerStorageAlias(factory)` adjacent to `registerStorageTimeSeries` — correctly left **unconditional** (only `TimeSeries` wrapped). `byte_equivalent: false` (decomposition: only the 5 `option` lines + line drift). Test `no_justified` (build-system-only; default-ON ⇒ upstream behavior; mirrors 045/051). Verified: build green (reconfigure confirmed), generated `config.h` all 5 = 1, flag↔option coverage diff empty, smoke resolves all gated surfaces, upstream `03222_create_timeseries_table` PASS. Dossier [`052-register-flags-newer-engines.md`](../../patches/052-register-flags-newer-engines.md). — additional compiler flags for newer engines/function | no |
| 053 | bb04848360 | 2026-01-09 | tilman.moeller@aiven.io | joelynch112@gmail.com | 1 | 16 | Check MergeTree settings constraints before enqueuing DDL queries — **COMMITTED `patch-port(053)` (`cd4f2ea2f13`)**. | no |
| 054 | 4c16ca65e9 | 2026-01-09 | tilman.moeller@aiven.io | joelynch112@gmail.com | 4 | 222 | Add read-only setting to KeeperMap storage — **COMMITTED `patch-port(054)` (`b7588ae1b56`)**, `still-needed-but-rewrite`. Adds a `read_only` Bool engine setting to `KeeperMap` (default `false`, inert): when on, `INSERT`/`TRUNCATE`/mutate throw `TABLE_IS_READ_ONLY` and `ALTER` is rejected unless settings/comment-only. Cherry-pick CONFLICTED at the `StorageKeeperMap` ctor (HEAD's Aiven-private `override_metadata` param occupies the slot the patch wanted) — resolved by keeping `override_metadata` and appending `keeper_map_settings_` as the final ctor arg at 3 sites. PLUS three 26.3 API adaptations the 25.8 source predates: `ASTPtr` → `#include <Parsers/IAST_fwd.h>` (intrusive_ptr), `std::make_shared<ASTSetQuery>` → `make_intrusive<ASTSetQuery>`, and the new `IDatabase::alterTable` `validate_new_create_query=true` arg. `byte_equivalent: false` (decomposition: only those four bounded changes + drift). Test = stateless `9054_keeper_map_read_only_setting`; evidence pair: pre-patch FAIL (KeeperMap rejects `SETTINGS`, code 36 `BAD_ARGUMENTS`), post-patch PASS. First add-new-TU patch (`KeeperMapSettings.{cpp,h}`) → needed a plain `cmake -B build` glob refresh; first KeeperMap test → needed a Keeper-enabled scratch server (see dossier §4/§6 + `build-and-test.md` §4). Dossier: `docs/aiven/patches/054-keeper-map-read-only-setting.md`. | yes (9054) |
| 055 | d6d957fd41 | 2026-01-09 | tilman.moeller@aiven.io | joelynch112@gmail.com | 25 | 163 | Add named_collection column to system.tables — **COMMITTED `patch-port(055)` (`34ca0cecbcb`)**, `still-needed`. Adds a `named_collection` `String` column to `system.tables`, surfaced from a new `IStorage::getNamedCollection` (a 3rd `IStorage` ctor param threaded through `StorageMySQL`/`StoragePostgreSQL`/`S3`/Azure ObjectStorage + the `MySQL`/`PostgreSQL` table functions). One bounded 26.3 adaptation: the new `IStorage` member is renamed `named_collection` → `named_collection_name` (accessor + ctor-param names unchanged) to dodge `-Wshadow-field -Werror` against new-in-26.3 engines (`YTsaurus`/`ArrowFlight`/`MongoDB`) whose own `named_collection` params the 25.8 source couldn't have covered — one base-class rename instead of chasing per-engine param renames. `byte_equivalent: false` (decomposition: that rename + non-semantic relocation/blank-line/brace drift, incl. `StorageBlocks.h` `LiveView/`→`WindowView/`). Test = stateless `9055_named_collection_system_tables`; evidence pair: pre-patch FAIL (`SELECT named_collection` → code 47 `UNKNOWN_IDENTIFIER`), post-patch PASS (`MySQL(nc)`→`nc`, control→`''`). `02117` schema snapshot updated (single `named_collection String,` line); `02206` confirmed unaffected. Build surfaced a `#deps 0` + `cmake --fresh` + sccache **stale-object** hazard (a `.o` predating the protected-users `AccessType.h` change crashed the server at startup via libc++ hardening OOB) → fixed by a touch-all-TUs rebuild; see dossier §6 + `build-and-test.md` §6. Dossier: `docs/aiven/patches/055-named-collection-system-tables.md`. | yes (9055) |
| 056 | b18cff803a | 2026-01-09 | tilman.moeller@aiven.io | joelynch112@gmail.com | 1 | 1 | Include metadata_version.txt when freezing | no |
| 057 | ac227df851 | 2026-01-12 | tilman.moeller@aiven.io | joelynch112@gmail.com | 16 | 610 | remove all cloud-specific settings | no |
| 058 | 9a2883592c | 2026-01-13 | tilman.moeller@aiven.io | joelynch112@gmail.com | 1 | 29 | Disallow replication parameters customization | no |
| 059 | 2f477f8b6d | 2026-01-13 | tilman.moeller@aiven.io | joelynch112@gmail.com | 2 | 9 | Fix ClickHouse trying to read non-existent certificate files If both the certificate file and key file are defined, even when they are not used, ClickHouse will try to read and parse them. We can't even work around the issue by creating empty files. — **PORTED `patch-port(059)` (`850d9b502b9`), `still-needed-but-rewrite`** (`byte_equivalent: false`). Base 26.3 has no secure-port gate so the premise holds, but the cherry-pick was reshaped: **(i) source Hunk 1 (`Server.cpp` `tryReloadAll`→`tryLoad`) DROPPED** — on 26.3 `tryReloadAll` loops over every registered prefix (server *and* client) and the swap would silently break client-cert hot-reload; the `tryLoadImpl` guard already covers all entry points. **(ii) guard SCOPED to `CFG_SERVER_PREFIX`** (`prefix == Poco::Net::SSLManager::CFG_SERVER_PREFIX && …`) so it fires only for server certs and never skips client-cert (outgoing mTLS) loading, which is independent of `tcp_port_secure`/`https_port`. Suppresses the spurious missing-cert read/parse when neither secure port is configured; clause-(v) narrow functional no-op (not gated). Ships integration test `test_aiven_lazy_certificates` (leg A: no-secure-port + bogus paths → guard log `No server certificates needed …`, no read attempt; leg B: `https_port` + valid cert → guard inert, HTTPS serves TLS) with a pre/post worktree-flip evidence pair. See dossier [`059-skip-unused-server-certificates.md`](../../patches/059-skip-unused-server-certificates.md). | no |
| 060 | 93c2be960f | 2026-01-13 | tilman.moeller@aiven.io | joelynch112@gmail.com | 1 | 3 | Fix alter order by `alter code` is detached from `create table` code, which makes it necessary to copy field initializaiton logic. This commit makes `alter table` to produce the same `sorting key` ZooKeeper metadata as `create table`. | yes |
| 061 | 285e6fde0f | 2026-01-21 | tilman.moeller@aiven.io | joelynch112@gmail.com | 4 | 131 | Changes for ClickHouse dictionary source, both remote and local. | no |
| 062 | 50a42b769e | 2026-01-29 | tilman.moeller@aiven.io | joelynch112@gmail.com | 5 | 21 | Prohibit .tmp table creation — **STAGED, head of the squashed chain `patch-port(062,063,064)`, `still-needed-but-rewrite`** (`byte_equivalent: false`). Reserves the `.tmp*` table-name namespace: 062's two throws (`InterpreterCreateQuery::doCreateTable` create-side, `InterpreterRenameQuery::executeToTables` rename-side, the latter only in `Replicated` DBs, keyed on `isInternalQuery`) are **gated** behind a new default-`false` server setting `aiven_prohibit_tmp_table_creation` (clause-(v); gate OFF = byte-identical to stock 26.3). The `setInternal`/`internal` member on `InterpreterRenameQuery` and the two internal-flips (`DatabaseReplicated::recoverLostReplica` create, `StorageMaterializedView::exchangeTargetTable` rename) ship unconditional. Drift: re-anchored 062's `#include <Common/StringUtils.h>` past HEAD's inserted `NamedCollectionsFactory.h`; added `Core/ServerSettings.h`; adapted the MV rename to the 066+078-refactored named-interpreter form. Squashed with 063+064 (strict-order; 062+063 without 064 is runtime-broken). Tests: integration `test_aiven_prohibit_tmp_table` (4/4 PASS: create-reject, rename-reject, CREATE OR REPLACE works, MV refresh works — each with OFF-state control) + stateless `09081_prohibit_tmp_table_creation_off_neutral` (OFF neutrality). See dossier [`062-prohibit-tmp-table-creation.md`](../../patches/062-prohibit-tmp-table-creation.md). | yes (test_aiven_prohibit_tmp_table, 09081) |
| 063 | cc718390aa | 2026-02-02 | alex.khatskevich@aiven.io | joelynch112@gmail.com | 1 | 4 | Use .tmp for all fake temporal tables — **STAGED, squashed into `patch-port(062,063,064)` (row 062), `still-needed-applies-cleanly`.** Ships **unconditional** (ungated): renames the `CREATE OR REPLACE` temp prefix `_tmp_replace_` → `.tmp_replace_` (literal + `^\.tmp_replace_(\w+)_(\w+)$` regex in `TemporaryReplaceTableName.cpp`) so it falls under the reserved `.tmp*` namespace. Neutrality verified (the only test referencing the old prefix, `test_refreshable_mv/test.py:190`, already filters `.`-prefixed names). | (squashed into 062) |
| 064 | c95ef0cc6b | 2026-02-05 | alex.khatskevich@aiven.io | joelynch112@gmail.com | 1 | 4 | asd .tmp create or replace (real change = the `CREATE OR REPLACE` internal exemption) — **STAGED, squashed into `patch-port(062,063,064)` (row 062), `still-needed-applies-cleanly`.** Ships **unconditional** (ungated): `doCreateOrReplaceTable` hoists the inner create into a named `interpreter`, calls `interpreter.setInternal(true)`, then `doCreateTable`, so the 062 guard exempts the engine's own `.tmp_replace_*` create. This is the commit that un-breaks the chain (062+063 without 064 throws on every `CREATE OR REPLACE`). | (squashed into 062) |
| 065 | 6fe07b9acd | 2026-01-29 | alex.khatskevich@aiven.io | joelynch112@gmail.com | 2 | 24 | Prohibit redirects from https to http — **PORTED `patch-port(065)` (`1b3f4784678`), `still-needed`** (clean cherry-pick, offsets only, `byte_equivalent: true`; no upstream `https`→`http` downgrade guard on 26.3; `UNACCEPTABLE_URL` (491) already exists. Faithful unconditional carry, NOT gated behind a server setting — clause-(v) blast radius is narrow (only a genuine downgrade during a redirect; plain-`http` URLs and same-scheme redirects unaffected) and security-positive. Guards both `ReadWriteBufferFromHTTP::callWithRedirects` (url()) and `PocoHTTPClient::makeRequestInternalImpl` (S3). Ships integration test `test_aiven_https_to_http_redirect` (not in source): localhost HTTPS/HTTP redirect subprocesses + `verificationMode=none`; post-patch 2 passed, pre-patch the rejection case fails because unpatched CH follows the downgrade and the SELECT returns data (control `http`→`http` passes both). url() leg runtime-tested; S3 hunk same idiom, by review. See dossier [`065-prohibit-https-to-http-redirect.md`](../../patches/065-prohibit-https-to-http-redirect.md). | no |
| 066 | 3866708bab | 2025-12-22 | alex.khatskevich@aiven.io | joelynch112@gmail.com | 4 | 1011 | Fix MV refresh in sharded environment — **STAGED, neutrality RESTORED `patch-port(066,078)`, `still-needed-but-rewrite`** (squashed with 078; not committed). Adds an Aiven-only shard-level coordination layer (global-leader/shard-leader, `…/shards/<shard>` Keeper tree, UUID-keyed `EXCHANGE` deferred until all shards finish) on top of upstream's replica-level coordination, fixing cross-shard data loss in sharded `DatabaseReplicated` refreshable MVs. 050-region reconciliation keeps `asyncTryCreateNoThrow` + no `MULTI_READ` check; new shard ops verified ZooKeeper-compatible (no multi-read). **Five** port-time defects fixed: null-deref in `createRefreshDirectory` (missing `!coordinated` guard); `#deps 0` layout-staleness garbling `system.view_refreshes` progress; and three single-node refresh-state-machine regressions — failed refreshes not recorded in the znode (so `exception` never cleared / `SYSTEM WAIT VIEW` gave a generic error), a dropped per-cycle `interrupt_execution` reset (cancelled views never re-ran), and missing `log_comment`/`try-catch` (failed refreshes absent from `system.query_log`; incl. the `process_list_entry` `weak_ptr` lifetime rule). The escalated "single-node hang" was disproved by an A/B vs a pre-066 base binary — a **test-harness artifact** (un-redirected `stdin` on `INSERT … VALUES`), not a 066 bug. Neutrality: **8/8** stateless refreshable-MV tests pass; the added 2-shard causation integration test passes end-to-end (`1 passed`, per-shard data retained). **Open item:** single-node vs coordinated error-reporting asymmetry from the failed-refresh fix (dossier §11), for maintainer decision. Added causation test `tests/integration/test_aiven_mv_refresh_sharded/` passes end-to-end on the local 066 binary. See dossier [`066-mv-refresh-sharded.md`](../../patches/066-mv-refresh-sharded.md). | no |
| 067 | d0a99ce495 | 2026-02-11 | tilman.moeller@aiven.io | joelynch112@gmail.com | 1 | 7 | Allow self signed certificates for local endpoints | yes |
| 068 | fc8f70cca6 | 2026-01-27 | joelynch112@gmail.com | joelynch112@gmail.com | 3 | 255 | Systemd logging — **COMMITTED `patch-port(068)` (`56d3e25ac73`)**, `still-needed-applies-cleanly`. Adds `SystemdJournalChannel` (journal native protocol over `/run/systemd/journal/socket`), selected for the first console channel when `JOURNAL_STREAM` is set and the socket connects (else falls back to `Poco::ConsoleChannel`); syslog priority mapping + multiline-safe binary `MESSAGE` + `CLICKHOUSE_*` fields. Clean cherry-pick (`byte_equivalent: true`), build green. Ships **no test** — human-approved `test_design_blocked` (no SQL/HTTP-visible surface; only effect is datagrams to a unix socket). Dossier: `docs/aiven/patches/068-systemd-logging.md`. | yes |
| 069 | 1502b77d88 | 2026-02-18 | joelynch112@gmail.com | joelynch112@gmail.com | 5 | 22 | Add zk_uptime to Keeper mntr four-letter command — **COMMITTED `patch-port(069)` (`a2d998c3ce9`)**, `still-needed-and-applies`. Adds a `zk_uptime` (ms) field to the Keeper `mntr` 4LW command, backed by an atomic `role_start_time` reset on `BecomeLeader`/`BecomeFollower`. Cherry-pick auto-merged clean despite two context-drift sites (`Keeper4LWInfo.h` gained `synced_non_voting_follower_count`; `getPartiallyFilled4LWInfo` reshaped by upstream `5000f02cef8`); `byte_equivalent: false` but decomposition shows no semantic delta. No upstream equivalent (`uptime` absent from `src/Coordination/` on HEAD). Test = assertion added to upstream integration `test_keeper_four_word_command::test_cmd_mntr` (`mntr` is Keeper-protocol, not SQL-observable); evidence pair: pre-patch FAIL `KeyError: 'zk_uptime'`, post-patch PASS. Dossier: `docs/aiven/patches/069-zk-uptime-keeper-mntr.md`. | no |
| 070 | d297c19ff8 | 2026-03-16 | alex.khatskevich@aiven.io | joelynch112@gmail.com | 2 | 4 | **COMMITTED — `patch-port(070)` (`8aac7c02c6a`)**, `partial`. Third link of the `REGISTER_*` family (precursor of 071). Source had 2 hunks; on our lineage only the `registerStorages.cpp` hunk is new — it wraps the previously-UNGUARDED `registerStorageYTsaurus(factory)` call in `#if USE_YTSAURUS … #endif` (its forward-decl was already `USE_YTSAURUS`-guarded, so a `USE_YTSAURUS=0` build now links). The source's `registerDictionaries.cpp` hunk was DROPPED — that dict source is already gated by `REGISTER_DICTIONARY_SOURCE_YTSAURUS` via committed 045 (staged dict diff confirmed empty, 045 not regressed); the `USE_YTSAURUS &&` prefix is added by 071. `byte_equivalent: false`. Test `no_justified` (`USE_YTSAURUS` defaults ON ⇒ no default-behavior change; build-correctness only). Verified: incremental build green, guard present, `system.table_engines` YTsaurus smoke = 1, upstream `01645_system_table_engines` PASS. Dossier [`070-disable-ytsaurus-engine.md`](../../patches/070-disable-ytsaurus-engine.md). — Disable YTsaurus engine | no |
| 071 | 1b350e6b65 | 2026-03-18 | alex.khatskevich@aiven.io | joelynch112@gmail.com | 4 | 9 | **COMMITTED — `patch-port(071)` (`533aa0fa40c`)**, `still-needed-but-rewrite`. Fourth link of the `REGISTER_*` family (chains onto committed 070; precursor of 075). Upgrades the YTsaurus guards to the two-level `#if USE_YTSAURUS && REGISTER_YTSAURUS_*` form and adds flags `REGISTER_YTSAURUS_TABLE_ENGINE` + `REGISTER_YTSAURUS_FUNCTION` (+ their `option(... ON)` in `configure_config.cmake`, PC-1). Two divergences vs the 25.8 source, both from earlier-committed siblings: (A) `REGISTER_DICTIONARY_SOURCE_YTSAURUS` flag + option already shipped in 045 → duplicate `config.h.in` line dropped (3-way merge deduped it), no second option; (B) the dict-source guard, `REGISTER_DICTIONARY_SOURCE_YTSAURUS` on HEAD, resolved to `USE_YTSAURUS && REGISTER_DICTIONARY_SOURCE_YTSAURUS` (prepend, not replace). `byte_equivalent: false`. Test `no_justified` (all flags + `USE_YTSAURUS` default ON ⇒ no default-behavior change). Verified: reconfigure+rebuild green, generated `config.h` all 3 YTSAURUS flags = 1, coverage diff empty, `system.table_engines`/`system.table_functions` YTsaurus smoke 1/1. Dossier [`071-register-ytsaurus-directives.md`](../../patches/071-register-ytsaurus-directives.md). — Add REGISTER_YTSAURUS directives | no |
| 072 | 92ab446d49 | 2026-03-26 | alex.khatskevich@aiven.io | joelynch112@gmail.com | 1 | 13 | Wait for distributed database creation | no |
| 073 | aec2378a0e | 2026-04-07 | alex.khatskevich@aiven.io | joelynch112@gmail.com | 1 | 5 | Fix compatibility setting crash on removed setting | yes |
| 074 | ab1fb1df41 | 2025-09-04 | vitlibar@clickhouse.com | joelynch112@gmail.com | 2 | 50 | **DROPPED — `obsoleted-by-upstream`.** `ab1fb1df41` is not Aiven-authored: it is upstream commit `12b0084e3e8` (same author Vitaly Baranov, same timestamp `2025-09-04 09:41:23 +0200`, identical diffstat +30/-20), which Aiven cherry-picked onto 25.8 because that base predated it. `git merge-base --is-ancestor 12b0084e3e8 v26.3.10.62-lts` confirms it is already in the 26.3 base; the post-image (`addressToArrowLocation` + bracketed-IPv6 `host_component`) is present in the base `ArrowFlightHandler.cpp`. Re-carrying it would duplicate a commit the base already has. Unrelated to the `REGISTER_*` family (045/051/052/070/071/075) — grouped with 075 only by the word "ArrowFlight". No code/commit/test. Dossier [`074-fix-arrowflight-ipv6-listen-host.md`](../../patches/074-fix-arrowflight-ipv6-listen-host.md). — Fix ArrowFlight support for IPv6 in listen_host. | n/a |
| 075 | 9650c1a6f5 | 2026-04-23 | alex.khatskevich@aiven.io | joelynch112@gmail.com | 3 | 8 | **STAGED — ready for commit (`patch-port(075)`)**, `still-needed-but-rewrite`. FINAL link of the `REGISTER_*` engine/function expose-gate family (chains onto committed 071; closes 045/051/052/070/071/075). ArrowFlight analogue of 071: adds flags `REGISTER_ARROWFLIGHT_TABLE_ENGINE` + `REGISTER_ARROWFLIGHT_FUNCTION` (+ their `option(... ON)` in `configure_config.cmake`, PC-1, appended by category to the end of the engines/functions block) and upgrades the three `#if USE_ARROWFLIGHT` guards (storage forward-decl, storage call, table function) to the two-level `#if USE_ARROWFLIGHT && REGISTER_ARROWFLIGHT_TABLE_ENGINE` form. Clean cherry-pick — no 045-style overlap (all guards matched the `#if USE_ARROWFLIGHT` pre-image; config.h.in anchors present from 071). Faithful-port quirk: `REGISTER_ARROWFLIGHT_FUNCTION` is defined + option'd but referenced by NO guard (the table-function guard uses `REGISTER_ARROWFLIGHT_TABLE_ENGINE`); kept verbatim for source/RPM-spec fidelity (documented in dossier §6). `byte_equivalent: false` (decomposition: only the 2 `option` lines). Test `no_justified` (all flags + `USE_ARROWFLIGHT` default ON ⇒ no default-behavior change; build-system-only). Verified: reconfigure + broad rebuild green, generated `config.h` both `REGISTER_ARROWFLIGHT_*` = 1 with `USE_ARROWFLIGHT` = 1, `system.table_engines`/`system.table_functions` ArrowFlight smoke 1/1. Dossier [`075-register-arrowflight-flags.md`](../../patches/075-register-arrowflight-flags.md). — Add register arrowflight flags | no |
| 076 | 7e09c63352 | 2026-04-29 | joelynch112@gmail.com | joelynch112@gmail.com | 9 | 294 | Add kafka_auto_offset_reset_ms setting for time-based consumer offset reset — **PORTED `patch-port(076)` (`33cd92d585b`), `still-needed-but-rewrite`** (the `DECLARE`d name is `kafka_auto_offset_reset_by_duration_ms`; the commit title abbreviates it). Adds a `UInt64` Kafka setting: when non-zero and a partition has no committed offset, the consumer starts from `now − duration_ms` (resolved via `librdkafka` offsets-for-times), else falls back to `kafka_auto_offset_reset`; `0` (default) = disabled = bit-for-bit prior behavior. Faithful setting + semantics, **rewritten** to 26.3: (i) **direct-wired** from `(*kafka_settings)[…]` at the consumer-construction sites — the source's `ConsumerConfigParams` field no longer exists (029–033 drift); (ii) `KafkaConsumer2` `make_shared` arg list adapted to 26.3's ctor (gained `skip_bytes_`), `auto_offset_reset_ms_` appended after it; (iii) `KafkaConsumer` assignment callback **mutates `TopicPartitionList` in place** (lambda widened to non-const ref) per `cppkafka` `handle_rebalance` assign-after-callback contract (verified in `contrib/cppkafka`); resolution wrapped in `try/catch (cppkafka::HandleException)` → graceful fallback. DDL-only stateless test `9076_kafka_offset_reset_by_duration` (accept/round-trip/default/large/coexist + non-numeric→`CANNOT_PARSE_INPUT_ASSERTION_FAILED`); `[ OK ]` post-patch, `UNKNOWN_SETTING` pre-patch (causation pair). Build-dir aside: an unrelated stale-`.o` link error (`IAccessStorage::insertImpl/removeImpl` vtables from committed 022, `#deps 0` per runbook §7) was cleared by force-recompiling the `src/Access` closure. See dossier [`076-kafka-offset-reset-by-duration.md`](../../patches/076-kafka-offset-reset-by-duration.md). | no |
| 077 | a25b337024 | 2026-04-30 | joelynch112@gmail.com | joelynch112@gmail.com | 4 | 36 | Hide secrets in system.mutations.command column | no |
| 078 | ea9c5d6420 | 2026-05-07 | alex.khatskevich@aiven.io | joelynch112@gmail.com | 2 | 69 | Fix MV refresh task race condition — **SQUASHED INTO `patch-port(066,078)`** (see row 066). 078's own body says "On version upgrade squash with commit 'Fix MV refresh in sharded environment'"; it only tunes 066's race-recovery timing (adds `isCurrentRefreshStillActive`, called once/sec in `getOrWaitForTemporaryTableID`) and is **coordinated-only** (no single-node effect; not implicated in the 066 neutrality work). Staged together with 066; neutrality restored and the 2-shard causation test passes end-to-end (see row 066). See dossier [`066-mv-refresh-sharded.md`](../../patches/066-mv-refresh-sharded.md). | no |

## Net-new patches (authored against 26.3)

These are **not** ported from the 25.8 source branch, so they have **no source-index
`NNN`** and no row in the mechanical table above. They are authored fresh against 26.3
(category D in [`commit-hygiene.md`](../../runbooks/commit-hygiene.md) §1), identified by
a per-cycle letter-prefixed `N<nn>` handle, and shipped with a `patch-new(N<nn>):`
commit. At the next LTS transition each folds into that uplift's mechanical source
inventory with a fresh `NNN`. Discovery context: [`new-surface-screening.md`](new-surface-screening.md).

| ID | authored | author | files | loc | status / description |
|---|---|---|---|---|---|
| N01 | 2026-06-12 | tilman.moeller@aiven.io | 4 | ~270 | **STAGED — ready for commit (`patch-new(N01)`).** Build-time `REGISTER_WEBASSEMBLY_UDF` gate (default ON via `option()` in `src/configure_config.cmake`) wrapping the single master-enable choke point `Context::initWasmModuleManager`, so the new-in-26.3 WebAssembly UDF subsystem (`CREATE FUNCTION ... LANGUAGE WASM` + `system.webassembly_modules`) can be compiled out independent of the runtime experimental setting `allow_experimental_webassembly_udf`. Orthogonal to `USE_WASMTIME`/`USE_WASMEDGE`. Test `no_justified` (build-system-only; default ON ⇒ no upstream-behavior change). Verified by a differential build: hardened `-DREGISTER_WEBASSEMBLY_UDF=0` → `config.h`=0, `system.webassembly_modules` absent + `CREATE ... LANGUAGE WASM` → `SUPPORT_IS_DISABLED` *even with the setting forced ON*; default → `config.h`=1, subsystem reachable (`RESOURCE_NOT_FOUND` on a missing module). Proposal [`2026-06-12-webassembly-udf-register-gate.md`](../../proposals/2026-06-12-webassembly-udf-register-gate.md); dossier [`N01-webassembly-udf-register-gate.md`](../../patches/N01-webassembly-udf-register-gate.md). |

## Summary

- Total: 78 patches
- `cherry_pick_clean=yes`: 25
- `cherry_pick_clean=no`: 53
- `cherry_pick_clean=error`: 0
- Authors (the person who originally wrote the patch):
  - `tilman.moeller@aiven.io`: 63
  - `alex.khatskevich@aiven.io`: 10
  - `joelynch112@gmail.com`: 4
  - `vitlibar@clickhouse.com`: 1 (upstream ClickHouse maintainer — patch 074, likely an upstream cherry-pick that may be in 26.3 already)
- Committers (the person who landed the patch on `v25.8.18.1-lts-aiven`,
  post-rebuild):
  - `joelynch112@gmail.com`: 57
  - `alex.khatskevich@aiven.io`: 21
- Date range (author date): 2025-12-02 to 2026-05-07 (~5 months of aiven-side work)

## Pointers

- T2 retrospective: `docs/aiven/uplifts/26.3/01-t2-classifier-retrospective.md`
- Classifier dispatch report (verbatim, scratch): `tmp/classifier/report.md`
- Dispatch prompt (verbatim, scratch): `tmp/classifier/dispatch-prompt.md`
- Plan: `docs/aiven/plans/2026-05-22-classifier-subagent.md`
- Design spec: `docs/aiven/proposals/2026-05-19-uplift-orchestration-design.md` §10 step 6, §14 Q6
- Subagent log: `docs/aiven/uplifts/26.3/log.md`
