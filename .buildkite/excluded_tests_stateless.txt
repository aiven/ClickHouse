# Stateless tests excluded on the Buildkite ephemeral runners.
#
# These are tests we intentionally do not run in the Buildkite stateless step.
# Most are environment limitations of the runner; entries that are excluded for a
# different reason have their own rationale below. The list is read by
# .buildkite/pipeline.yml and forwarded to `clickhouse-test --skip`, which matches
# by SUBSTRING against the test name. One pattern per line; blank lines and lines
# starting with '#' are ignored.

# IPv6 loopback (::1) is not available in the test container
01880_remote_ipv6
02479_mysql_connect_to_self
02784_connection_string

# The container filesystem does not support O_DIRECT
03595_pread_threadpool_direct_io

# Needs an interactive pty (expect)
02931_client_fuzzy_search_crash

# The checkout path contains "clickhouse", which the test rewrites to "ch"
03536_ch_as_client_and_local

# Known flaky on this branch/runner: sometimes reports different MergeTree part
# hashes after `ALTER TABLE ... REWRITE PARTS`. Keep separate from the environment
# exclusions so it is easy to revisit.
03706_statistics_preserve_checksums_on_mutations
